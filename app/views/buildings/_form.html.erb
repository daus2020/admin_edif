<div class="card login mx-auto">
  <h3 class = "text-center mt-2">Edificio</h3>
    <%= image_tag 'building.png', alt: 'building', class: "mt-1 img_login" %>
  <div class="card-body">
    <%= form_with(model: building) do |form| %>
      <% if building.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(building.errors.count, "error") %> no permite(n) guardar:</h2>
          <ul>
            <% building.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%# <%= form.label :name %>
        <h6 class= "mb-0">Nombre</h6>
        <%= form.text_field :name %>
      </div>

      <div class="field">
        <%# <%= form.label :address %>
        <h6 class= "mt-3 mb-0" >Dirección</h6>
        <%= form.text_area :address %>
      </div>

      <div class="field">
        <%# <%= form.label :city %>
        <h6 class= "mt-3 mb-0">Ciudad</h6>
        <%= form.text_field :city %>
      </div>

      <div class="actions">
        <%= form.submit class: "btn btn-outline-primary mt-2", title: 'Actualizar' %>
        
      </div>
    <% end %>
  </div>
</div>

