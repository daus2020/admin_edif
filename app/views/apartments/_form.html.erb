<%= form_with(model: [@apartment.building, @apartment]) do |f| %>
  <% if @apartment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apartment.errors.count, "error") %> que no permiten guardar:</h2>

      <ul>
      <% @apartment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h6>Número</h6>
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <h6 class= "mt-3">Status</h6>
    <%= f.select :status, Apartment::STATUS_OPTIONS %>
  </div>

  <div class="actions mt-4">
    <%= f.submit "Ingresar", class: "btn btn-outline-primary" %>
  </div>
<% end %>
